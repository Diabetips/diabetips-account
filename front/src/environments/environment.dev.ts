import { Environment } from '../environment';

export const environment: Environment = {
  production: true,

  apiUrl: 'https://api.dev.diabetips.fr',
  backUrl: 'https://account.dev.diabetips.fr',
};
